#wap to calculate area of rectangle based on length and breadth.

length = float(input('Enter length of rectangle :'))
breadth = float(input('Enter breadth of rectangle :'))

Area = length * breadth
print(f'Area of reactangle is {Area}.')